basic

# Tests not using object orientation
functional

# Tests for object-oriented code
objects

# Check we can capture exceptions that are thrown and replay them
exceptions

# Tests for interception involving packaged modules
packages

# tests for only intercepting certain calls to a module
# 
partial_interception
